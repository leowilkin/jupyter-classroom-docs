# Jupyter Classroom Documentation

This site is the source for [Jupyter Classroom](https://pypi.org/project/jupyter-classroom/)'s documentation.

## Development

Install packages with `npm i` and run with `npm run dev`.
